pipeline {
    agent any
    stages {
        stage ('Initialize') {
            steps {
               bat 'echo %PATH%'
			   bat 'echo %M2_HOME%'
            }

            
        }

        stage ('Deploy Build in Staging Area'){
            steps{
                      echo 'Deployement Failure on PRODUCTION'
            }
        }

        
    }
}
